# Bugs Pendientes — Post-Refactor Simplificación

Extraído de `ANALISIS-REFACTOR-SIMPLIFICACION.md` y `AUDITORIA-ARQUITECTURA-v2.md`. Verificado contra código. Estado al commit `230eabd`.

| # | Problema | Detalle | Estado |
|---|---|---|---|
| 1 | **Tests de integración faltantes** | Se agregaron tests de integración reales (MongoMemoryServer + supertest) para booking completo, cancelación/reprogramación, administración de profesionales, consulta anónima de turnos y tempLock. Commits: `2b330ee`, `8537bdd`, `a243971` (2026-06-22). | ✅ Real — Resuelto |
| 2 | **Bug: Race condition en tempLock** | ~~Dos usuarios pueden crear tempLock simultáneamente para el mismo slot.~~ El unique index `{ barberId, date, startTime }` + catch error 11000 impide esa condición exacta. El bug real es otro: el flujo completo (crear turno → borrar lock → email) no usa transacciones MongoDB, no hay atomicidad entre pasos, y el `.catch(() => {})` al borrar el lock traga errores silenciosamente. | ⚠️ Parcial (mitigado) — descripción original incorrecta. El race condition de creación no ocurre, pero falta transaccionalidad |
| 3 | **Bug: tempLock sin autenticación de ownership** | La ruta DELETE `/temp-lock/:tempLockId` no tiene middleware `authenticate`, el controller nunca lee `req.user`, y el modelo ni siquiera almacena quién creó el lock. Cualquier cliente que conozca un ObjectId (guessable) puede liberar un lock ajeno. | ❌ Real — sin mitigación |
| 4 | **Frontend: resolver dualidad service/RTK Query** | ~~~10+ archivos~~ duplicados. Son exactamente **2 archivos API** (`authApi.ts`, `appointmentApi.ts`) que duplican endpoints de **2 services** (`auth.service.ts`, `appointment.service.ts`). Los otros 2 services (`professional`, `service`) no tienen duplicación. ~8-10 endpoints duplicados, no archivos. | ❌ Real — ~10 endpoints duplicados en 4 archivos |
| 5 | **Transacciones MongoDB** | `CreateAppointmentUseCase` nunca crea `mongoose.startSession()`. Los 3 pasos (crear turno → borrar lock → email) son operaciones independientes sin atomicidad ni rollback. El borrado del lock tiene `.catch(() => {})` que traga errores. | ❌ Real — sin transacciones en el flujo crítico |
| 6 | **14 archivos `mock.ts` con lorem ipsum** | Archivos placeholder en `components/`, `constants/`, `context/`, `hooks/`, `layout/`, `pages/`, `scripts/`, `services/`, `types/`, `utils/`. Contienen solo un string de lorem ipsum. **Ninguno es importado** por otro módulo. En `context/` y `layout/` son el único archivo del directorio. | ❌ Real — código 100% muerto. Acción: borrar los 14 archivos |
| 7 | **12 directorios vacíos** | Quedaron como scaffolding tras el refactor: `use-cases/service/`, `use-cases/tempLock/`, `use-cases/user/`, `domain/repositories/`, `infrastructure/mappers/`, `interface-adapters/presenters/`, y 6 directorios de tests sin implementar. Confunden la navegación del proyecto. | ❌ Real — acción: eliminar directorios |
| 8 | **`PasswordStrength.tsx` duplicado** | Existe en `components/common/` (46 líneas, sin animación) y en `pages/public/RegisterPage/components/` (55 líneas, con framer-motion). La lógica `getStrength()` es idéntica, el renderizado es diferente (uno con motion.div, otro con div plano). Dos implementaciones distintas del mismo componente. | ❌ Real — unificar en una sola versión |
| 9 | **Controllers con múltiples responsabilidades** | `BarberController` tiene 10 métodos, recibe 7 dependencias, y mezcla validación VOs, lógica de negocio, envío de emails y manejo HTTP. El `delete()` es el peor: cancela turnos, borra tempLocks y envía emails directamente. | ⚠️ Aceptado — riesgoso si el dominio crece, pero viable hoy |
| 10 | **props público en Appointment** | ~~`appointment.props.status = 'Cancelado'` puede hacerse sin pasar por `cancel()`.~~ `props` es **TypeScript `private`**. Todo el código respeta encapsulación. Solo se muta mediante `cancel()`, `pay()`, `complete()`, `markNoShow()` que validan transiciones vía `VALID_TRANSITIONS`. En runtime JS se podría forzar con `any`, pero es riesgo teórico. Ver `analisis-readonly-props.md` para plan de agregar `readonly` a todas las Props del dominio (solo `Appointment` requiere adaptación, 8 asignaciones). | ✅ Falso positivo — la encapsulación se respeta en la práctica |
| 11 | **Acoplamiento a implementaciones concretas** | `RegisterUserUseCase` recibe `MongoUserRepository` (concreto), no `IUserRepository`. Solo `IPasswordHasher` e `IEmailService` usan interfaces. El resto depende de clases concretas. | ⚠️ Aceptado — sin plan de cambiar BD, testing con MongoDB Memory Server |
| 12 | **Pérdida de documentación de tipos** | Los DTOs inline (10 tipos definidos dentro de use-cases) **no tienen JSDoc**. Los validators Joi (4 archivos) documentan la estructura esperada con mensajes en español. Se perdió documentación de negocio legible por humanos, no la validación técnica. | ⚠️ Mitigado — Joi compensa la ausencia de JSDoc en los tipos inline |
| 13 | **~60 tests ceremoniales o duplicados sin valor** | Auditoría del 2026-06-23 sobre los 76 archivos de test del proyecto (38 backend + 38 frontend) reveló: **Backend**: 13 tests de controllers que solo verifican `res.status(200/500)` — la lógica real ya está en use cases. **Frontend**: 4 archivos duplicados (2 pares service ↔ RTK Query: `appointment.service.test` + `appointmentApi.test`, `auth.service.test` + `authApi.test`) — ~36 tests que verifican 2 veces las mismas llamadas HTTP. 2 tests ceremoniales (`AnimatedContainer.test.tsx`, `StepIndicator.test.tsx` recovery). Ratio ruido/señal: ~18% del total de tests no aporta capacidad de detección de bugs. | ❌ Real — ~60 tests eliminables sin pérdida de cobertura real. |
