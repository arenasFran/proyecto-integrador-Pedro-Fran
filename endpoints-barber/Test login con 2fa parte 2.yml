info:
  name: Test login con 2fa parte 2
  type: http

http:
  method: POST
  url: http://localhost:3000/auth/2fa/verify
  body:
    type: json
    data: |-
      {
        "email": "test@gmail.com",
        "code": "791389"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​