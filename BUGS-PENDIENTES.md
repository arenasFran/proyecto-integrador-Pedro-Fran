# Bugs Pendientes — Post-Refactor Simplificación

Extraído de `ANALISIS-REFACTOR-SIMPLIFICACION.md` y `AUDITORIA-ARQUITECTURA-v2.md`. Verificado contra código. Estado al commit `230eabd`.

| # | Problema | Detalle | Estado |
|---|---|---|---|
| 1 | **Tests de integración faltantes** | Se agregaron tests de integración reales (MongoMemoryServer + supertest) para booking completo, cancelación/reprogramación, administración de profesionales, consulta anónima de turnos y tempLock. Commits: `2b330ee`, `8537bdd`, `a243971` (2026-06-22). | ✅ Real — Resuelto |
| 2 | **Bug: Race condition en tempLock** | ~~Dos usuarios pueden crear tempLock simultáneamente para el mismo slot.~~ El unique index `{ barberId, date, startTime }` + catch error 11000 impide esa condición exacta. El bug real es otro: el flujo completo (crear turno → borrar lock → email) no usa transacciones MongoDB, no hay atomicidad entre pasos, y el `.catch(() => {})` al borrar el lock traga errores silenciosamente. | ⚠️ Parcial (mitigado) — descripción original incorrecta. El race condition de creación no ocurre, pero falta transaccionalidad |
| 3 | **Bug: tempLock sin autenticación de ownership** | La ruta DELETE `/temp-lock/:tempLockId` no tiene middleware `authenticate`, el controller nunca lee `req.user`, y el modelo ni siquiera almacena quién creó el lock. Cualquier cliente que conozca un ObjectId (guessable) puede liberar un lock ajeno. | ❌ Real — sin mitigación |
| 4 | **Frontend: resolver dualidad service/RTK Query** | ~~~10+ archivos~~ duplicados. Son exactamente **2 archivos API** (`authApi.ts`, `appointmentApi.ts`) que duplican endpoints de **2 services** (`auth.service.ts`, `appointment.service.ts`). Los otros 2 services (`professional`, `service`) no tienen duplicación. ~8-10 endpoints duplicados, no archivos. | ❌ Real — ~10 endpoints duplicados en 4 archivos |
| 5 | **Transacciones MongoDB** | `CreateAppointmentUseCase` nunca crea `mongoose.startSession()`. Los 3 pasos (crear turno → borrar lock → email) son operaciones independientes sin atomicidad ni rollback. El borrado del lock tiene `.catch(() => {})` que traga errores. | ❌ Real — sin transacciones en el flujo crítico |
| 6 | **14 archivos `mock.ts` con lorem ipsum** | Archivos placeholder en `components/`, `constants/`, `context/`, `hooks/`, `layout/`, `pages/`, `scripts/`, `services/`, `types/`, `utils/`. Contienen solo un string de lorem ipsum. **Ninguno es importado** por otro módulo. En `context/` y `layout/` son el único archivo del directorio. | ❌ Real — código 100% muerto. Acción: borrar los 14 archivos |
| 7 | **12 directorios vacíos** | Quedaron como scaffolding tras el refactor: `use-cases/service/`, `use-cases/tempLock/`, `use-cases/user/`, `domain/repositories/`, `infrastructure/mappers/`, `interface-adapters/presenters/`, y 6 directorios de tests sin implementar. Confunden la navegación del proyecto. | ❌ Real — acción: eliminar directorios |
| 8 | **`PasswordStrength.tsx` duplicado** | Existe en `components/common/` (46 líneas, sin animación) y en `pages/public/RegisterPage/components/` (55 líneas, con framer-motion). La lógica `getStrength()` es idéntica, el renderizado es diferente (uno con motion.div, otro con div plano). Dos implementaciones distintas del mismo componente. | ❌ Real — unificar en una sola versión |
| 9 | **Controllers con múltiples responsabilidades** | `BarberController` tiene 10 métodos, recibe 7 dependencias, y mezcla validación VOs, lógica de negocio, envío de emails y manejo HTTP. El `delete()` es el peor: cancela turnos, borra tempLocks y envía emails directamente. | ⚠️ Aceptado — riesgoso si el dominio crece, pero viable hoy |
| 10 | **props público en Appointment** | ~~`appointment.props.status = 'Cancelado'` puede hacerse sin pasar por `cancel()`.~~ `props` es **TypeScript `private`**. Todo el código respeta encapsulación. Solo se muta mediante `cancel()`, `pay()`, `complete()`, `markNoShow()` que validan transiciones vía `VALID_TRANSITIONS`. En runtime JS se podría forzar con `any`, pero es riesgo teórico. Ver `analisis-readonly-props.md` para plan de agregar `readonly` a todas las Props del dominio (solo `Appointment` requiere adaptación, 8 asignaciones). | ✅ Falso positivo — la encapsulación se respeta en la práctica |
| 11 | **Acoplamiento a implementaciones concretas** | `RegisterUserUseCase` recibe `MongoUserRepository` (concreto), no `IUserRepository`. Solo `IPasswordHasher` e `IEmailService` usan interfaces. El resto depende de clases concretas. | ⚠️ Aceptado — sin plan de cambiar BD, testing con MongoDB Memory Server |
| 12 | **Pérdida de documentación de tipos** | Los DTOs inline (10 tipos definidos dentro de use-cases) **no tienen JSDoc**. Los validators Joi (4 archivos) documentan la estructura esperada con mensajes en español. Se perdió documentación de negocio legible por humanos, no la validación técnica. | ⚠️ Mitigado — Joi compensa la ausencia de JSDoc en los tipos inline |
| 13 | **~60 tests ceremoniales o duplicados sin valor** | Auditoría del 2026-06-23 sobre los 76 archivos de test del proyecto (38 backend + 38 frontend) reveló: **Backend**: 13 tests de controllers que solo verifican `res.status(200/500)` — la lógica real ya está en use cases. **Frontend**: 4 archivos duplicados (2 pares service ↔ RTK Query: `appointment.service.test` + `appointmentApi.test`, `auth.service.test` + `authApi.test`) — ~36 tests que verifican 2 veces las mismas llamadas HTTP. 2 tests ceremoniales (`AnimatedContainer.test.tsx`, `StepIndicator.test.tsx` recovery). Ratio ruido/señal: ~18% del total de tests no aporta capacidad de detección de bugs. | ❌ Real — ~60 tests eliminables sin pérdida de cobertura real. |
| 14 | **Bypass de validación de entidad en DeleteBarberUseCase** | `DeleteBarberUseCase.ts:29-35` cancela turnos futuros llamando a `appointmentRepository.updateStatus()` directo, sin pasar por `appointment.cancel()`. Esto bypasea la máquina de estados (`VALID_TRANSITIONS`), no registra en `statusHistory`, y si cambian las reglas de cancelación en el futuro este código quedará desactualizado. Solo opera sobre `Confirmado` (transición válida hoy), pero es una bomba de tiempo. | ❌ Real — sin mitigación |
| 15 | **`slotDuration` con default en 6 lugares distintos** | El valor por defecto `30` está definido en: Joi validator (`barber.validator.ts:56`), Mongoose schema Employee (`barber.model.ts:153`), Mongoose schema Admin (`barber.model.ts:190`), MongoRepository (`MongoBarberRepository.ts:38`), Controller (`BarberController.ts:93`), Domain Service (`SlotService.ts:11,98-101`). La entidad `Barber.create()` **no tiene default** (a diferencia de `maxAdvanceDays` que sí). Cambiar el default requiere modificar 6 lugares — alta probabilidad de inconsistencia. | ❌ Real — riesgo de bugs futuros |
| 16 | **Email validado en 4 capas con REGEX distintos** | La validación de email aparece en: Joi (usa `EMAIL_REGEX`), Value Object `Email.ts` (misma constante), Controller (`BarberController.ts:66` re-valida post-Joi), y Frontend (`constants/validation.ts:3` con regex **diferente**). El frontend usa `/^[^\s@]+@[^\s@]+\.[^\s@]+$/` que no es equivalente al backend. Puede haber casos donde frontend acepte y backend rechace (o viceversa). | ❌ Real — validación inconsistente frontend vs backend |
| 17 | **Password validado en 5 capas** | La misma política de password (min 8, mayúscula, minúscula, dígito) se valida en: Joi (`auth.validator.ts:6-11`), Value Object `Password.ts:6-18`, Controller (`BarberController.ts:68` re-valida), Frontend constants (`validation.ts:7-14`), y Frontend hook (`useFormValidation.ts:11-17`). Joi usa `min(6)` mientras Password exige `min(8)` — si Joi aprueba algo que Password rechaza, el error llega al usuario tarde y confuso. | ❌ Real — inconsistencia entre capas (Joi min:6 vs VO min:8) |
| 18 | **Lógica de solapamiento de turnos duplicada** | La secuencia completa de validación de tiempo (parsear startTime, calcular endMinutes con `barber.slotDuration`, validar horario laboral, breaks, colisiones) estaba implementada **dos veces**: en `CreateAppointmentUseCase.ts` y `RescheduleAppointmentUseCase.ts`. Se extrajo a `validateAppointmentSlot()` en `domain/utils/time.ts`. Ambos use cases ahora llaman a la misma función. | ✅ Real — Resuelto. Extraído a `domain/utils/time.ts:106-141`. |
| 19 | **Nombre de método engañoso: `findAllBarbers`** | `MongoBarberRepository.ts:86` — `findAllBarbers()` usa el filtro `{ kind: { $in: ['Empleado', 'Admin'] } }`, retorna tanto empleados como administradores. El nombre sugiere que solo retorna barberos (empleados), pero incluye admins. El controller compensa filtrando en memoria (`BarberController.ts:109-113`), pero es técnicamente incorrecto y puede confundir a futuros desarrolladores. | ⚠️ Menor — confusión de naming, no bug funcional |
| 20 | **Ausencia de tokens CSRF** | No hay ningún mecanismo de CSRF (ni `csurf`/`csrf` package, ni doble-submit cookie, ni custom token). La única mitigación es `SameSite=strict` en la cookie del refresh token (`TwoFactorController.ts:12`), que solo protege en navegadores modernos. Apps móviles, Postman, o integraciones externas no tienen protección. `SameSite` tiene bugs conocidos en Safari. | ❌ Real — riesgo de CSRF en navegadores no-Chromium y APIs |
| 21 | **Emails de notificación fire-and-forget sin retry** | Los emails de negocio (creación, cancelación, reprogramación, baja de barbero) usan `.catch(console.error)` sin reintento, sin cola, sin feedback al usuario. Si el SMTP falla, el turno se crea igual en BD pero el cliente nunca recibe la confirmación. Solo los emails de 2FA y reseteo de password tienen retry con backoff y error propagation. Archivos: `CreateAppointmentUseCase.ts:250-261`, `CancelAppointmentUseCase.ts:88-98`, `RescheduleAppointmentUseCase.ts:163-173`, `DeleteBarberUseCase.ts:39-47`, `UpdateAppointmentStatusUseCase.ts:116-135`. | ❌ Real — pérdida silenciosa de notificaciones |
| 22 | **XSS en templates de email** | Los datos ingresados por usuarios (nombres, apellidos, motivo de cancelación, nombre de servicio, precio) se interpolan directamente en templates HTML de email sin escape ni sanitización. Ejemplo: `CancelAppointmentUseCase.ts:92-93` interpola `reason` (input del usuario) directamente en `<p>Motivo: ${reason}</p>`. Un atacante puede inyectar `<script>` o `<img onerror>` que se ejecute en clientes de email que renderizan HTML/JS. Afecta a 5 archivos: `CreateAppointmentUseCase.ts`, `CancelAppointmentUseCase.ts`, `RescheduleAppointmentUseCase.ts`, `DeleteBarberUseCase.ts`, `UpdateAppointmentStatusUseCase.ts`. | ❌ Real — vulnerabilidad de seguridad activa |
| 23 | **Fragmentación de identidad (dual-collection) y riesgo de enumeración** | `MongoUserRepository.ts:104-117` busca secuencialmente `Barber.findOne()` → `RegisteredClient.findOne()`, creando timing side-channel medible (barbero = 1 query, cliente = 2 queries). `VerifyTwoFactorUseCase.ts:29` usa mensaje diferenciado `'Usuario no encontrado.'` que permite inferir si un email existe en el sistema. ADR-017 documenta la decisión pero no mitiga el vector de enumeración. | ❌ Real — riesgo de seguridad activo |
| 24 | **TTL index faltante en colección passwordresets** | `passwordReset.model.ts` (líneas 1-23) no define ningún índice TTL. `refresh_tokens` (`refreshToken.model.ts:20`) y `templocks` (`tempLock.model.ts:36`) sí lo tienen. Los documentos expirados de reseteo de contraseña se acumulan indefinidamente, degradando rendimiento de consultas y aumentando costos de almacenamiento. Fix: agregar `passwordResetSchema.index({ expiresAt: 1 }, { expireAfterSeconds: 0 })`. | ❌ Real — fix de 1 línea |
| 25 | **Lockout y contadores de intentos en MongoDB (no en Redis)** | `twoFactorFailedAttempts`, `twoFactorLockedUntil`, `resetFailedAttempts` y `resetLockedUntil` se persisten en documentos MongoDB (`barber.model.ts:79-94`, `client.model.ts:96-111`). Un ataque DoS distribuido contra `/auth/2fa/verify` genera miles de escrituras por segundo en MongoDB. La capa de rate limiting (en memoria) absorbe parte, pero los fallos que la cruzan escriben directo a BD. | ⚠️ Riesgo de escalabilidad — aceptable hoy, problemático en producción con carga real |
| 26 | **Fricción del 2FA obligatorio sin dispositivo de confianza** | El 2FA es obligatorio para todo login local sin excepción (`ADR-010`). No existe mecanismo de "dispositivo de confianza" ni "recordar este equipo". Cada inicio de sesión exige leer un código de 6 dígitos del email con ventana de 5 minutos vía SMTP, que puede tener demoras impredecibles. Afecta especialmente a barberos que operan el sistema en el local y necesitan acceso rápido. | ⚠️ Aceptado — decisión de seguridad deliberada, pero con impacto UX documentado |
| 27 | **Seed analytics fragmentado en 4 sub-comandos** | El seed de analytics tenía sub-comandos `heatmap`, `distribucion` y `reservasganancias` creados incrementalmente por cada chart del dashboard, en vez de un seed único con datos realistas. Causaba errores E11000 por solapamiento de franjas, estado inconsistente si un sub-comando fallaba, y datos diseñados "a medida" por componente. | ✅ Real — Resuelto. Unificado en un solo seed con ~175 turnos, 73 fechas, 3 barberos, 4 servicios y 7 meses de histórico. |
| 28 | **Falta validación de alineación de slots en creación y reprogramación** | `CreateAppointmentUseCase.ts` y `RescheduleAppointmentUseCase.ts` calculaban `endMinutes = startMinutes + barber.slotDuration` pero nunca validaban que `startTime` estuviera alineado con `slotDuration` respecto al inicio de la jornada. Aceptaban horas como 10:20 con slotDuration=60 (slots válidos: 10:00, 11:00...). La validación se incorporó dentro de `validateAppointmentSlot()` al resolver #18. | ✅ Real — Resuelto. Incluido en `validateAppointmentSlot()` en `domain/utils/time.ts`. |
| 29 | **Turnos cancelados/no-show aparecen en gráfico "Cantidad de Turnos" y truncado de minutos en eje X** | `getReservasGanancias()` hacía `{ $sum: 1 }` sin filtrar por estado, contando cancelados. Además, `{ $substrCP: ['$startTime', 0, 2] }` truncaba los minutos (14:30 → 14:00). Fix: agregar `$nin: ['Cancelado', 'NoShow']` en `$match` por defecto, y usar `'$startTime'` directo como periodo intra-día. | ✅ Real — Resuelto. |
| 30 | **Rate limiting faltante en analytics** | Los endpoints `/analytics/*` no tienen middleware de rate limiting. Un atacante puede consultar agregaciones pesadas (overview, heatmap, distribución) repetidamente sin restricción, potencialmente degradando la BD. El rate limiting general de la app está en `auth` y `booking`, pero analytics quedó fuera. | ❌ Real — sin mitigación |
| 31 | **Domain importa de Application (`AppError`)** | `Appointment.ts`, `Email.ts`, `Password.ts`, `Phone.ts` y `time.ts` (todos en `domain/`) importaban `AppError` desde `application/errors/AppError`, violando la regla de dependencia de Clean Architecture. Se movió `AppError.ts` a `domain/errors/`. Actualizados ~30 imports en 5 archivos del dominio + 14 de aplicación + 5 de adaptadores + 1 infraestructura + 1 common + 16 tests. | ✅ Real — Resuelto. `AppError` movido a `domain/errors/AppError.ts`. |
| 32 | **`catch(() => {})` silencioso en fetch de bloques** | `CalendarPage/index.tsx:124` — el fetch de bloques falla silenciosamente sin `console.error`. Si la API de bloques está caída, el calendario se renderiza sin bloques y el admin no tiene feedback visual. Puede llevar a crear turnos en horarios bloqueados creyendo que están libres. | ❌ Real — sin mitigación |
| 33 | **`toPrimitives()` expone timestamps mutables por referencia** | `Appointment.ts:74-76` — `toPrimitives()` hace `return { ...this.props }`, copia superficial. Los `Date` internos (createdAt, updatedAt, cancelledAt, statusHistory[].timestamp) se copian por referencia. Quien recibe el objeto puede mutar las fechas del Appointment interno sin pasar por los métodos de dominio. | ❌ Real — sin mitigación |
